[internal]

; Dial r sends 180/183 only when callee is actually ringing (no fake early Ringing).
exten => _10XX,1,NoOp(Call ${EXTEN} from ${CALLERID(num)})
 same => n,Dial(PJSIP/${EXTEN}@${EXTEN},30,r)
 same => n,Hangup()
