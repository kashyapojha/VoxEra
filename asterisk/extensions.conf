[internal]

; Ringing() → SIP 180 to caller; Dial r → ringback while callee rings.
exten => _10XX,1,NoOp(Call ${EXTEN} from ${CALLERID(num)})
 same => n,Ringing()
 same => n,Dial(PJSIP/${EXTEN},30,r)
 same => n,Hangup()
